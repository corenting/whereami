name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    container:
      image: nimlang/nim
      
    steps:
    - uses: actions/checkout@v1
    - name: make build-release
      run: make build-release
